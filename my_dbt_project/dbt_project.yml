name: my_dbt_project
version: '1.0'
config-version: 2
profile: my_profile

model-paths:
  - models

macro-paths:
  - macros

models:
  my_dbt_project:
    +materialized: view
    marts:
      +materialized: table
